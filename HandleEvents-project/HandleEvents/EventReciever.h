#pragma once
bool GetFileEvents()
{
    while (true)
    {
        //Call FltGetMessage();
        //push data to event publisher list

        /*if (is_thread_exit_event)
        {
            break;
        }*/
    }
}